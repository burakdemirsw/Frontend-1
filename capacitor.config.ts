import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'beatport',
  webDir: 'dist/Frontend-Beatzone-main',
  bundledWebRuntime: false
};

export default config;
