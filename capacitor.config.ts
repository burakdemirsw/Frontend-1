import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devdavtic.angular',
  appName: 'beatport',
  webDir: 'dist/beatport',
  bundledWebRuntime: false
};

export default config;
