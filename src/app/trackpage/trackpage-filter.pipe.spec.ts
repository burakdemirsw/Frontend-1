import { TrackpageFilterPipe } from './trackpage-filter.pipe';

describe('TrackpageFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TrackpageFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
