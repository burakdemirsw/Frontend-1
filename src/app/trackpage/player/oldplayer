 <!-- old player
<div class="music_player" *ngIf="buttonClicked">
  <div class="music_player_body_up">

    <input
      type="range"
      min="0"
      max="{{ duration }}"
      [value]="seek"
      (click)="setCurrentTime($event)"
      class="form-range col-xl"
      id="audioRange2"
    />
  </div>

  <div>
    <div class="music_player_body_down">
      <div class="row">
        <div class="music_detail">
          <img
            src="{{ trackDetails[4].photoUrl }}"
            style="max-width: 100%; height: 100%"
          />
        </div>
        <div class="music_detail_1">
          <h5>{{ trackDetails[0].title }}</h5>
        </div>

        <div class="music_player_buttons">
          <button class="btn btn-dark " (click)="skipStart()">
            <i class="bi bi-skip-start"></i>
          </button>

          <button class="btn btn-dark  ml-3" (click)="play()">
            <i class="bi bi-play"></i>
          </button>
          <button class="btn btn-dark ml-3" (click)="pause()">
            <i class="bi bi-pause"></i>
          </button>
          <button class="btn btn-dark ml-3" (click)="stop()">
            <i class="bi bi-stop"></i>
          </button>

          <button class="btn btn-dark ml-3" >
            <i class="bi bi-skip-end"></i>
          </button>
        </div>

        <div class="music_player_buttons_1">
          <button
            class="btn btn-dark"
            (click)="volumeUp()"
            id="volumeFull"
            name="volumeFull"
          >
            <i class="bi bi-volume-up" id="volumeUp" name="volumeUp"></i>
          </button>
          <button
            class="btn btn-dark"
            (click)="mute()"
            id="volumeMute"
            name="volumeMute"
          >
            <i
              class="bi bi-volume-mute-fill"
              id="volumeMute"
              name="volumeMute"
            ></i>
          </button>
          <input
            type="range"
            min="0"
            max="1"
            step="0.01"
            value="1"
            (click)="setVolume($event)"
            class="form-range"
            id="audioRange"
          />
        </div>
      </div>
    </div>
  </div>
</div> -->
