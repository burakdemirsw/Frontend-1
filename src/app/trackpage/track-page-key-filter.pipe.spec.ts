import { TrackPageKeyFilterPipe } from './track-page-key-filter.pipe';

describe('TrackPageKeyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TrackPageKeyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
